import { beforeEach, describe, expect, it, vi } from 'vitest';
describe('managed transmit store', () => { beforeEach(() => vi.resetModules()); it('marks cached countdown stale and never changes server debt', async () => { const store = await import('../managed-transmit.svelte'); store.receiveManagedTransmitSnapshot({ schemaVersion: 1, sampledAt: 'x', managedTransmit: { status: 'available', intent: { kind: 'rx' }, releaseRequired: true, lastError: null, lastActuation: null, abortErrors: [], tot: { configuredSeconds: 180, active: true, remainingMs: 100, expiresAt: 'x' } }, txObservation: { observedPtt: 'off' } }, 10); expect(store.managedTransmitRemainingMs(20)).toBe(90); store.invalidateManagedTransmit(); expect(store.managedTransmitRemainingMs(20)).toBeNull(); expect(store.managedTransmitSnapshot()?.managedTransmit.status === 'available' && store.managedTransmitSnapshot()!.managedTransmit.releaseRequired).toBe(true); }); });
